import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Next.js는 자동으로 .env 파일을 로드하므로 env 섹션이 필요 없음
  // 서버 사이드 환경 변수는 자동으로 process.env에 로드됨
};

export default nextConfig;
